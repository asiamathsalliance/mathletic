import ProfileActivityClient from "./ProfileActivityClient";

export const metadata = { title: "Activity | Mathletic" };

export default function ProfileActivityPage() {
  return <ProfileActivityClient />;
}
