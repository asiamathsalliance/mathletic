import { SprintEntrySkeleton } from "@/components/sprint/SprintEntrySkeleton";

/** Landing / mode-select page — not the in-session play shell. */
export default function Loading() {
  return <SprintEntrySkeleton />;
}
